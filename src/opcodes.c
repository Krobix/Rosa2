#include "opcodes.h"

/*Used for storing opcode string table for conversion, apparently you shouldnt define variables in header files*/
const char rosa2_opcode_char[] = "noop add sub mul div strstore intstore flstore dictdef stdout stdin strload intload flload dictload stradd fladd halt mov";